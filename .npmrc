# React Native (Expo 54) must use the same react version as react-native-renderer (19.1.0).
# Clerk/Next peer ranges omit 19.1.0 but it works; relax peers so the monorepo can install.
legacy-peer-deps=true
