PORT=3000
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/railway
# Railway Postgres usually requires TLS — set true if your driver needs an explicit flag
DATABASE_SSL=true
CLERK_SECRET_KEY=sk_test_...
